<h3>Create Category</h3>

<%= form_for @category do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div> 
    
    <%= f.submit "Create Category", class:"custom_button" %>
  <% end %>
  <br>

<!-- see if i can pass data into the path below so it can auto-populate -->
<!-- or path with a query param within the url -->
  <%= link_to "Create Recipe with Category", new_category_recipe_path(@category.recipes), class: "custom_button" %>

